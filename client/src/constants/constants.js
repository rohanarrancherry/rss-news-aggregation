export const DEFAULT_ENDPOINT = '/api/feeds';
export const DEFAULT_PAGE = 1;
export const LIMIT = 25;
export const MIN_IMAGE_WIDTH = 250;
export const TTL = 1000 * 60 * 15; // 15 minutes, used for caching

// fetch the categories from the db for each users
export const CATEGORIES = [
'latest',
'technology',
'Sports'
];
